# Contracts

Reserved for future Solidity contracts, generated ABIs, or deployment notes.

The current static frontend reads the CASHX PRC-20 contract through `lib/contracts.js` and `lib/pulsechain.js`.
